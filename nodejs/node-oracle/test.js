var oracle = require('./build/Release/oracle_win32');var oracleoperator = new oracle.OracleOperator(10);try{var ret;ret = oracleoperator.connect("zhs","nodejs","nodejs");console.log(ret);//ret = oracleoperator.getError();//console.log('error:'+ ret);var array;array = oracleoperator.query('select * from test where id=?',3);if(array != false){console.log("ret:"+ array.length);	for(var i=0; i < array.length;i++){		console.log(array[i]);	}}//console.log(ret);/*var sql='insert into test values(?,?,?)';ret = oracleoperator.execute(sql,'232313','张三',20);console.log(ret);ret = oracleoperator.beginTran();sql='insert into test values(?,?,?)';ret = oracleoperator.execute(sql,'232313','李四',20);ret = oracleoperator.rollBack();ret = oracleoperator.beginTran();sql='insert into test values(?,?,?)';ret = oracleoperator.execute(sql,'232313','李四',20);ret = oracleoperator.commit();*/}catch(e){	console.log('exerer ' +e);}